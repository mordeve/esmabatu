import 'package:flutter/material.dart';

class DugunPage extends StatelessWidget {
  const DugunPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Welcome to the Page Wedding"),
      ),
    );
  }
}
